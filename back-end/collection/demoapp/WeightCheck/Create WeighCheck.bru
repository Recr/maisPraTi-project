meta {
  name: Create WeighCheck
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/user/weight-check
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "weight": 40.0
  }
}
